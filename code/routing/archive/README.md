# Routing code archive

This local-only folder contains superseded working versions retained for
comparison. These files are not part of the active workflow and are ignored by
Git.

The active scripts are one level above:

- `routing_algorithm_time_matrix.py`
- `select_fastest_od_pairs.py`
- `routing_algorithm_itineraries.py`

Do not run an archived script unless investigating historical behavior.
